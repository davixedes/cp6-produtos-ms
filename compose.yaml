services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: produtosdb
    ports:
      - "3307:3306"
    volumes:
      - produtos_mysql_data:/var/lib/mysql

volumes:
  produtos_mysql_data:
